# 2026-02-19

- 사용자 요청으로 `baskduf/ssafy_quest` private repo 접근용 SSH 키 생성 및 GitHub 등록 완료.
- 서버에서 repo 클론 완료: `/home/ubuntu/workspace/ssafy_quest`
- README 전면 개편 후 커밋/푸시 완료.
  - commit: `3201e14`
  - message: `docs: rewrite README with project overview and setup`

## 면접 준비 요약 (프로젝트 기반)

### 프로젝트 한 줄 설명
- SSAFY 학생들의 알고리즘 학습 지속을 위해 만든 게임화 플랫폼 (랭킹/퀘스트/레이드/길드/맵).

### 쉬운 설명 포인트
- SSAFY 로그인으로 사용자 인증
- solved.ac 데이터 연동으로 점수/랭킹 반영
- 문제를 풀면 레이드 보스 HP 감소
- 길드 경쟁/일일퀘스트로 재방문 유도

### DB 설계 핵심 포인트 (면접 대비)
1. **User 중심 도메인 확장 구조**
   - User를 중심으로 Raid, Guild, DailyQuest, Item 연결
2. **복합 Unique로 무결성 보장**
   - 예: `raidId+userId`, `userId+questDate`, `guildId+userId`
3. **N:M 관계를 조인 테이블로 분리**
   - `RaidParticipation`, `UserItem`, `GuildRegionScore`
4. **Enum 상태값으로 규칙 고정**
   - RaidStatus, GuildJoinRequestStatus 등
5. **트랜잭션으로 정합성 확보**
   - 레이드 공격/HP 차감/보상 지급을 원자적으로 처리

### 30초 답변 템플릿
- “User 중심으로 도메인을 분리했고, 다대다는 조인 테이블로 풀어 확장성과 정합성을 확보했습니다. 복합 unique 제약으로 중복을 차단하고, 레이드 보상처럼 원자성이 필요한 로직은 트랜잭션으로 처리했습니다.”

- User requested a new permanent assistant character direction based on an All Might-themed prompt (heroic/confident "I am here" vibe).
